import styled from 'styled-components';

export const FormFields = styled.div`
    display: grid;
    align-content: start;
    grid-area: fields;
    grid-gap: var(--grid-gap);
    overflow: auto;
`;
